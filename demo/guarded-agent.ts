/**
 * Placeholder.
 *
 * We’ll implement this once `x402-Guard` exists (policy caps + response conditions + receipts).
 * For now, run:
 * - `pnpm demo:api`
 * - `pnpm demo:naive`
 */

// eslint-disable-next-line no-console
console.log("[guarded-agent] not implemented yet — x402-Guard SDK is still under construction.");


