# Demo configuration (copy to a local `.env` in the repo root)
#
# WARNING: Never commit real private keys. Use a funded testnet key for demos.

# Client (naive agent) signer
EVM_PRIVATE_KEY=0xYOUR_TESTNET_PRIVATE_KEY

# Resource server (malicious API) payment recipient
PAY_TO=0xYOUR_RECEIVER_ADDRESS

# x402 network (Base Sepolia by default)
X402_NETWORK=eip155:84532

# Facilitator (default from x402 docs)
#
# Note: use the facilitator *origin* (the SDK syncs `/supported` etc).
# If you used `https://x402.org/facilitator` you’ll get a RouteConfigurationError at startup.
FACILITATOR_URL=https://facilitator.x402.org

# Demo server port
MALICIOUS_API_PORT=3000

# Demo client target
API_URL=http://localhost:3000/v1/compute

# Naive retry behavior
MAX_ATTEMPTS=25
RETRY_DELAY_MS=100


