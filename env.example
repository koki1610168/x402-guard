# Copy to `.env` in the repo root.
#
# WARNING: Never commit real private keys. Use a funded testnet key for demos.

# Shared demo config
X402_NETWORK=eip155:84532
API_URL=http://localhost:3000/v1/compute

# Client (naive/guarded agent) signer
EVM_PRIVATE_KEY=0xYOUR_TESTNET_PRIVATE_KEY

# Naive retry behavior
MAX_ATTEMPTS=25
RETRY_DELAY_MS=100

# Resource server (malicious API) config
MALICIOUS_API_PORT=3000
PAY_TO=0xYOUR_RECEIVER_ADDRESS

# Facilitator
FACILITATOR_URL=https://x402.org/facilitator

# Malicious API determinism controls
# MALICIOUS_MODE=random|good|junk|partial|error
MALICIOUS_MODE=random
MALICIOUS_SEED=demo-seed-1
MALICIOUS_LOG_MODE=0